# Student submissions

Create one folder here named exactly after your GitHub username. Copy the task's
`TEMPLATE.md` into that folder as `README.md`, and add the required task files.

Example:

```text
submissions/
└── octocat/
    ├── README.md
    └── environment.txt
```

Do not edit another student's folder.
